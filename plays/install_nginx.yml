---
  - include: install_rpm.yml
    vars:
      rpm_app_name: NGINX
      rpm_filename: nginx-1.6.3-6.el7.x86_64.rpm

#  - name: Starting the NGINX Service
#    service:
#      name=nginx
#      state=started
#      enabled=yes