---
  - name: Deploy Jenkins to CI Environment
    include: target_host1.yml

  - name: Deploy Artifactory to CI Environment
    include: target_host2.yml

  - name: Deploy GOGS to CI Environment
    include: target_host3.yml

  - name: Deploy SonarQube to CI Environment
    include: target_host4.yml
