---
  # Store IP addresses for use by Jenkins role on ci-a
  - copy: content={{hostvars['ci-b']['ansible_host']}} dest=cache/ci-b-ip.txt

  - copy: content={{hostvars['ci-dit']['ansible_host']}} dest=cache/ci-dit-ip.txt